# Abhängigkeiten sind in pyproject.toml deklariert (PEP 621).
# Installation für Entwicklung/Vollbetrieb:
#
#     pip install -e .[viewer,scraping,vision,dev]
#
# Nur Kern (GEDCOM-Analyse + Ancestry-Download, ohne Flask-Viewer,
# Playwright-Crawler und Claude-Vision-Transkription):
#
#     pip install -e .
#
-e .[viewer,scraping,vision,dev]
